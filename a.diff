--- sekai-7/mmre/MMRE-ai/communication/include/IServer.hpp
+++ sekai-7/mmre/MMRE-ai/communication/include/IServer.hpp
@@ -14,7 +14,8 @@
 struct IpcResponse {
     common::StatusCode status;
     std::string textualPayload; 
-    std::vector<memory::SharedMemoryHandle> shmHandles; 
+    // 统一命名空间：修复跨域冲突，统一使用 common::SharedMemoryHandle
+    std::vector<common::SharedMemoryHandle> shmHandles; 
 };
 
 class IServer {
@@ -34,7 +35,7 @@
      */
     virtual void PushEvent(const std::string& topic, 
                            const std::vector<uint8_t>& eventPayload,
-                           const std::vector<memory::SharedMemoryHandle>& shmHandles) = 0;
+                           const std::vector<common::SharedMemoryHandle>& shmHandles) = 0;
 };
 
 } // namespace communication

--- sekai-7/mmre/MMRE-ai/memory/include/SharedMemoryPtr.hpp
+++ sekai-7/mmre/MMRE-ai/memory/include/SharedMemoryPtr.hpp
@@ -21,14 +21,21 @@
     const common::SharedMemoryHandle& GetHandle() const;
 
     /**
-     * @brief Grants read/write access to the actual zero-copy payload in current process space.
-     * Guaranteed to be valid as long as this object is alive.
+     * @brief Const-correctness 修复：防止只读快照被意外修改。
      */
-    void* GetRawData() const { return mappedMemory_.get(); }
+    const void* GetRawData() const { return mappedMemory_.get(); }
+    
+    /**
+     * @brief 获取可变指针（仅限于构建/反序列化阶段使用）。
+     */
+    void* GetRawData() { return mappedMemory_.get(); }
     
     /**
      * @brief Convenience operator to cast data directly to required struct/type.
      */
+    template<typename T>
+    const T* As() const { return static_cast<const T*>(GetRawData()); }
+
     template<typename T>
     T* As() const { return static_cast<T*>(GetRawData()); }

--- sekai-7/mmre/MMRE-ai/client/include/EngineClient.hpp
+++ sekai-7/mmre/MMRE-ai/client/include/EngineClient.hpp
@@ -3,28 +3,41 @@
 #include <string>
 #include <functional>
+#include <vector>
 #include "Types.hpp"
-#include "SharedMemoryHandle.hpp"
+#include "SharedMemoryPtr.hpp"
 
 namespace mmre {
 namespace client {
 
+/**
+ * @brief 包含多模态数据的富查询结果 DTO。
+ */
+struct SqlQueryResult {
+    std::string textualPayload;
+    std::vector<memory::SharedMemoryPtr> blobData; // SDK 内部自动完成映射，直接提供可用指针
+};
+
+/**
+ * @brief 触发器事件的多模态载荷 DTO。
+ */
+struct TriggerEvent {
+    std::string eventMetadata;
+    std::vector<memory::SharedMemoryPtr> shmData; // 零拷贝数据，例如触发时的 Camera 帧
+};
+
 class EngineClient {
 public:
     explicit EngineClient(const std::string& connectionUri);
     ~EngineClient();
 
     bool Connect();
 
-    std::string QuerySql(const std::string& sqlQuery);
+    SqlQueryResult QuerySql(const std::string& sqlQuery);
 
-    std::vector<common::SharedMemoryHandle> QueryBlobRange(const std::string& resourceId, 
-                                                           common::TimestampNs start, 
-                                                           common::TimestampNs end);
+    std::vector<memory::SharedMemoryPtr> QueryBlobRange(const std::string& resourceId, 
+                                                        common::TimestampNs start, 
+                                                        common::TimestampNs end);
 
-    void SubscribeTrigger(const std::string& eventName, std::function<void(const std::string&)> callback);
+    void SubscribeTrigger(const std::string& eventName, std::function<void(const TriggerEvent&)> callback);
 
     void InjectCheckpoint(const std::string& description);

--- sekai-7/mmre/MMRE-ai/query/include/QueryEngine.hpp
+++ sekai-7/mmre/MMRE-ai/query/include/QueryEngine.hpp
@@ -48,6 +48,17 @@
      */
     void CloseCursor(uint64_t cursorId);
 
+    /**
+     * @brief 游标心跳保活。Agent 在长查询过程中调用以刷新 TTL，防止超时。
+     */
+    void KeepAliveCursor(uint64_t cursorId);
+
+    /**
+     * @brief 扫描并清理超时（TTL 过期）的游标。
+     * 可由内部定时器或内存压力监测器异步触发，彻底杜绝崩溃导致的内存泄漏。
+     */
+    void EvictStaleCursors();
+
 private:
     std::shared_ptr<engine_core::TimelineCache> cache_;
     std::shared_ptr<engine_core::PersistenceEngine> persistence_;

--- sekai-7/mmre/MMRE-ai/ingestion/include/IResourceProvider.hpp
+++ sekai-7/mmre/MMRE-ai/ingestion/include/IResourceProvider.hpp
@@ -5,12 +5,30 @@
 #include <variant>
 #include "Types.hpp"
-#include "SharedMemoryPtr.hpp" // 移除对底层核心数据结构的依赖
 
 namespace mmre {
 namespace ingestion {
 
 /**
+ * @brief 硬件无关的原生内存块抽象。
+ * 允许 Provider 直接操作内存，但对底层实现（堆内存或共享内存）完全无感。
+ */
+struct ProviderBuffer {
+    void* data{nullptr};
+    size_t capacity{0};
+    size_t filledSize{0}; // Provider 写入数据后更新此字段
+};
+
+/**
+ * @brief 注入给 Provider 的分配器接口，由 Engine 实现并传入。
+ * 实现控制反转 (IoC)，完美解耦 HAL 与 内存池。
+ */
+class IAllocator {
+public:
+    virtual ~IAllocator() = default;
+    virtual ProviderBuffer Allocate(size_t sizeHint) = 0;
+};
+
+/**
  * @brief Hardware-agnostic representation of ingested data.
  */
 struct RawProviderData {
@@ -20,7 +38,7 @@
     uint32_t subResourceIdHash;
     uint32_t pluginSchemaId{0};
     
-    std::variant<common::PayloadBuffer, memory::SharedMemoryPtr> payload;
+    std::variant<common::PayloadBuffer, ProviderBuffer> payload; // 解耦完成
 };
 
@@ -37,8 +55,10 @@
     virtual void SetPushCallback(OnDataPushedCallback callback) = 0;
 
     /**
      * @brief Start capturing hardware data on a specific fine-grained channel.
+     * 增加 allocator 参数，向硬件通道注入内存分配能力。
      */
-    virtual void StartChannel(uint32_t subResourceIdHash, const std::string& channelConfigJson) = 0;
+    virtual void StartChannel(uint32_t subResourceIdHash, const std::string& channelConfigJson, 
+                              std::shared_ptr<IAllocator> allocator) = 0;
 
     virtual void StopChannel(uint32_t subResourceIdHash) = 0;
 };